# Optional: override the Chrome executable path for local development.
# If not set, the app will try common OS-specific paths automatically.
# CHROME_EXECUTABLE_PATH=/usr/bin/google-chrome

# Set NODE_ENV=production when deploying to Vercel (handled automatically).
# NODE_ENV=production
